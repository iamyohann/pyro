def identity<T>(x: T) -> T:
    return x

def process<T, U>(a: T, b: U):
    print(a)
    print(b)

record Box<T>(value: T)

record Result<T, E>(ok: T, error: E)

type ID = int | string

def print_id(id: ID):
    print(id)

let x = Box(10)
let y = Box("hello")

print(identity(10))
print(identity("world"))

process(1, "two")
print_id(123)
print_id("abc")
