let l: list = [1, 2, 3]
print(l)

let t: tuple = (1, 2)
print(t)

let s: set = {1, 2}
print(s)

let d: dict = {"a": 1, "b": 2}
print(d)

let ml: list_mut = ListMutable([1, 2])
print(ml)

let mt: tuple_mut = TupleMutable((1, 2))
print(mt)

let ms: set_mut = SetMutable({1, 2})
print(ms)

let md: dict_mut = DictMutable({"a": 1})
print(md)
